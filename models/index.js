const User = require('./User.js'); // Import the User model
const Thought = require('./Thought.js'); // Import the Thought model

module.exports = { User, Thought }; // Export an object containing the User and Thought models
